import { NgModule } from '@angular/core';

import { ColorCompressionService } from './color-compression.service';

@NgModule({
  providers: [ColorCompressionService],
})
export class ColorCompressionModule { }
